# /recap — Session 2026-07-06

**Branch:** `claude/personal-guardian-design-tm11yf` · **Focus:** Personal Guardian ("החונך") module — design phase, no code.

## Topic
Kick off the Personal Guardian module per the continuation brief: audit the existing
codebase, produce the `/alt3` 3-step design plan, implement Step 1 (design foundations +
child 30-second dashboard spec), and red-team it. Owner approved proceeding after the
plan was presented ("תתקדם איפה שניתן").

## Completed this session
1. **Codebase audit** — repo contains "Aura Slim v1.0": Hebrew/RTL single-file React app
   on **Vite (not Next.js)**. Two critical discoveries:
   - Both app versions call the Anthropic API **from the browser with no API key** →
     every AI call fails 401; no backend exists (`App.jsx:90` and inline in `index.html`).
   - Root `index.html` is a standalone Babel-in-browser copy that Vercel actually serves;
     the Vite `App.jsx`/`main.jsx` at repo root are dead code (correct `src/` layout only
     exists inside `aura-deploy.zip`). Last two commits were hand-patches to that
     standalone file — that was the previous stopping point.
2. **`00-alt3-plan.md`** — 3-step plan: (1) foundations + dashboard spec, (2) Guardian
   conversation design, (3) implementation blueprint. One `/redteam` gate per step.
3. **`01-design-foundations.md`** — Step 1 implemented: personas (child ~12, parent),
   benchmark "never-ship" clutter list, safety boundaries SB-1…SB-10, child/parent IA
   with hard isolation, the 30-Second Dashboard spec (hierarchy, wireframe, states,
   30-second test as acceptance criterion), 9-screen MVP inventory as scope-creep gate.
4. **`02-redteam-step1.md`** — 9 findings (1 critical, 2 high, 4 medium, 2 low); all
   resolved in the design or converted to named gates. **Step 1 passed.**
5. Adult modules untouched, zero application code written — per protocol.

## Open issues (owner decisions)
1. **Backend timing** — server-side AI proxy is mandatory (SB-8/RT-1): build it inside
   Step 3, or as a separate infra fix first? (Also un-breaks the adult simulator.)
2. **Transcript granularity** (SB-6): full transcripts vs. topic summaries for the
   parent. Recommendation: full transcripts for MVP.
3. **Real store audit** of competitor apps before public launch (current benchmark is
   desk research, disclosed as such).
4. Repo hygiene (not urgent, pre-Step-3): root layout is inconsistent with the Vite
   build; deployed artifact is the standalone `index.html`.

## Update (same day, after owner approval to continue)
- **Step 2 completed and passed `/redteam`:**
  - `04-conversation-design.md` — master prompt v1 (8 hard boundaries), pedagogical
    rules, 5 core flows (first-run fully scripted), allowlist/blocklist, 9 failure &
    abuse flows.
  - `05-redteam-step2.md` — findings RT-10…RT-18; all resolved in the design or gated
    to Step 3. Fixes were folded back into `04`.
- Side-quest resolved: suspected "work from yesterday" — verified via local git,
  GitHub API (all branches/commits) and owner's account screenshot: account
  `alonkos22` has exactly one repo (`aura`) and nothing was pushed after 25.05
  except this session's work. Owner confirmed no Cursor in use; nothing to recover.

## Next steps
- **Step 3 — Implementation blueprint** (needs owner approval): stack decision
  (Vite + serverless vs. Next.js), component map, data model, parent controls,
  acceptance checklist. Then `/redteam`, then code.
- **Blocking inheritances for Step 3:** server-side AI proxy (RT-1); distress-detection
  mechanism with fail-safe (RT-12); server-computed numbers, model never does ledger
  math (RT-13); per-child context keying + PIN-gated profile switch (RT-14);
  semantic-first output filter (RT-16).
- Open owner decisions unchanged: backend timing (§8.1), transcript granularity (§8.2),
  real store audit before launch (§8.3).
